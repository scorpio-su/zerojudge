#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int n;
    while (cin >> n){
        cout <<"\'C\' can use printf(\"%d\",n); to show integer like "<<n << endl;
    }
    return 0;
}
